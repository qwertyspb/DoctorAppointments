@model IEnumerable<WebDoctorAppointment.Models.AppointmentViewModel>

@{
	ViewData["Title"] = "Назначения";
	var dateFrom = (DateTime)ViewData["DateFrom"];
	var dateTill = (DateTime)ViewData["DateTill"];
}

<h3>Назначения с @dateFrom.ToShortDateString() по @dateTill.ToShortDateString() </h3>

<p>
	<a asp-action="Create">Добавить назначение</a>
</p>

<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.DoctorName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.PatientName)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.StartTime)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.EndTime)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.DoctorName)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.PatientName)
				</td>
				<td>
					@Html.DisplayFor(model => item.StartTime)
				</td>
				<td>
					@Html.DisplayFor(model => item.EndTime)
				</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.Id">Изменить</a> |
					<a asp-action="Delete" asp-route-id="@item.Id">Удалить</a>
				</td>
			</tr>
		}
	</tbody>
</table>
<p>
	<a asp-action="IndexFull">Показать все назначения</a>
</p>
